---
import Layout from "../layouts/Layout.astro";
import Navbar from "../components/Navbar.astro";
import Footer from "../components/Footer.astro";
import AstroLogo from "../assets/logos/astro-logo-light-gradient.svg";
import PHPLogo from "../assets/logos/php-logo.svg";
import GoLogo from "../assets/logos/go-logo.svg";
import GoFiberLogo from "../assets/logos/go-fiber-logo.svg";
import JavascriptLogo from "../assets/logos/javascript-logo.svg";
import VueJSLogo from "../assets/logos/vue-js-logo.svg";
import LaravelLogo from "../assets/logos/laravel-logo.svg";
import PostgreSQLLogo from "../assets/logos/postgresql-logo.svg";
import MySQLLogo from "../assets/logos/mysql-logo.svg";
import { Image } from "astro:assets";

const detailedSkills = [
  {
    category: "Languages",
    description: "The core foundations of my technical stack. I prioritize type safety, performance, and clean syntax.",
    items: [
      { name: "PHP", logo: PHPLogo, level: "Advanced", years: "4+ years", detail: "Expertise in modern PHP (8.x+), focusing on performance and robust backend systems." },
      { name: "Go", logo: GoLogo, level: "Advanced", years: "2+ years", detail: "Building high-concurrency microservices and fast API gateways with Go." },
      { name: "JavaScript", logo: JavascriptLogo, level: "Advanced", years: "4+ years", detail: "Proficient in modern ES6+, TypeScript, and building interactive frontend experiences." },
    ],
  },
  {
    category: "Frameworks",
    description: "Tools that help me build scalable applications faster without compromising on quality.",
    items: [
      { name: "Laravel", logo: LaravelLogo, level: "Expert", years: "4+ years", detail: "Deep understanding of the Laravel ecosystem, including Eloquent, Queues, and Service Containers." },
      { name: "Go Fiber", logo: GoFiberLogo, level: "Advanced", years: "2+ years", detail: "Building lightweight, high-performance web applications and REST APIs." },
      { name: "Vue.js", logo: VueJSLogo, level: "Advanced", years: "3+ years", detail: "Creating reactive and component-based user interfaces with Vue 3 and Composition API." },
      { name: "Astro", logo: AstroLogo, level: "Advanced", years: "1+ year", detail: "Building content-driven websites with modern island architecture for optimal performance." },
    ],
  },
  {
    category: "Databases",
    description: "Relational database management systems I use to ensure data integrity and high-performance querying.",
    items: [
      { name: "PostgreSQL", logo: PostgreSQLLogo, level: "Advanced", years: "3+ years", detail: "Experience with complex indexing, JSONB usage, and advanced performance tuning." },
      { name: "MySQL", logo: MySQLLogo, level: "Advanced", years: "4+ years", detail: "Robust experience in architecting and managing relational schemas for enterprise applications." },
    ],
  },
];

const secondarySkills = [
  "Git & GitHub", "Docker", "Redis", "REST APIs", "TypeScript", "Tailwind CSS", "Unit Testing", "CI/CD", "Linux", "Nginx"
];
---

<Layout title="Skills & Expertise — Akmal Keisin">
  <Navbar />

  <!-- ── Dark Library Hero ─────────────────────────────────────────────────── -->
  <section class="relative bg-moss-800 overflow-hidden pt-[66px]" aria-label="Skills hero">
    <!-- Background textures -->
    <div class="absolute inset-0 pointer-events-none overflow-hidden">
      <svg class="absolute inset-0 w-full h-full opacity-[.04]">
        <defs>
          <pattern id="hex-skills" x="0" y="0" width="60" height="52" patternUnits="userSpaceOnUse">
            <path d="M30 2L56 17V47L30 62L4 47V17Z" fill="none" stroke="#c8f0d0" stroke-width="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#hex-skills)" />
      </svg>
      <div class="absolute top-0 right-0 w-120 h-120 rounded-full bg-moss-600/20 blur-3xl" aria-hidden="true"></div>
      <div class="absolute bottom-0 left-0 w-80 h-80 rounded-full bg-mint/10 blur-3xl" aria-hidden="true"></div>
    </div>

    <div class="relative max-w-6xl mx-auto px-6 py-20 md:py-28">
      <div class="reveal">
        <span class="block w-12 h-[3px] rounded-full mb-6 bg-gradient-to-r from-mint to-moss-500"></span>
        <p class="text-label-mono text-moss-300 mb-4">Technical Expertise</p>
        <h1 class="text-hero-title text-fog">
          My specialized<br/><em class="not-italic text-mint">toolkit</em>
        </h1>
        <p class="text-hero-subtitle text-moss-300 mt-8 max-w-xl">
          Four years of rigorous practice and production experience across the full web stack. I build systems that are as efficient as they are elegant.
        </p>
      </div>

      <!-- Stats row -->
      <div class="flex flex-wrap gap-10 md:gap-20 mt-16 pt-12 border-t border-moss-700/50">
        {[
          ["4+", "Years Exp."],
          ["10+", "Technologies"],
          ["100%", "Autodidact"],
        ].map(([num, label], i) => (
          <div class={`reveal d${i + 1}`}>
            <div class="text-hero-title text-fog">{num}</div>
            <div class="text-label-mono text-moss-400 mt-2">{label}</div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ── Detailed Skill Sections ───────────────────────────────────────────── -->
  <main class="bg-fog">
    <div class="max-w-6xl mx-auto px-6 py-24">
      {detailedSkills.map((cat, ci) => (
        <section class="mb-32 last:mb-0">
          <!-- Category Header -->
          <div class={`reveal mb-12 flex flex-col md:flex-row md:items-end gap-6 md:gap-16`}>
            <div class="md:w-1/3">
              <div class="flex items-center gap-3 mb-4">
                <svg width="16" height="16" viewBox="0 0 14 14" fill="none" aria-hidden="true">
                  <path d="M7 1L13 4.5V10.5L7 14L1 10.5V4.5Z" fill="#3e7d32" opacity="0.1" stroke="#3e7d32" stroke-width="1.5"/>
                </svg>
                <h2 class="text-label-mono text-moss-500 font-bold">{cat.category}</h2>
              </div>
              <p class="text-section-desc text-moss-600 italic">
                {cat.description}
              </p>
            </div>
            <div class="flex-1 h-px bg-sage-200 mb-2 hidden md:block"></div>
          </div>

          <!-- Skill Cards -->
          <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            {cat.items.map((item, ii) => (
              <div class={`proj-card reveal reveal-d${(ii % 3) + 1} bg-white border border-sage-200 rounded-3xl p-8 hover:shadow-xl hover:border-moss-200 transition-all duration-500 flex flex-col justify-between overflow-hidden relative group`}>
                <!-- Decorative top gradient -->
                <div class="absolute top-0 right-0 w-32 h-32 bg-gradient-to-bl from-mint/10 to-transparent rounded-bl-[100%] opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
                
                <div>
                  <div class="flex items-center justify-between mb-8">
                    <div class="w-14 h-14 bg-fog rounded-2xl flex items-center justify-center border border-sage-100 group-hover:scale-110 transition-transform duration-500">
                      <Image src={item.logo} alt={item.name} class="w-8 h-8 object-contain" />
                    </div>
                    <span class="text-label-mono text-moss-400 border border-sage-200 px-3 py-1 rounded-full">
                      {item.level}
                    </span>
                  </div>
                  
                  <h3 class="text-card-title text-ink mb-4">{item.name}</h3>
                  <p class="text-section-desc text-moss-600 text-sm">
                    {item.detail}
                  </p>
                </div>

                <div class="mt-8 pt-6 border-t border-sage-100 flex items-center justify-between">
                  <span class="text-label-mono text-moss-300">{item.years}</span>
                  <svg width="18" height="18" viewBox="0 0 18 18" fill="none" class="text-mint opacity-40">
                    <path d="M9 2L11.5 7L17 7.5L13 11.5L14 17L9 14.5L4 17L5 11.5L1 7.5L6.5 7L9 2Z" fill="currentColor"/>
                  </svg>
                </div>
              </div>
            ))}
          </div>
        </section>
      ))}

      <!-- ── Secondary Skills Strip ────────────────────────────────────────── -->
      <section class="mt-40 reveal">
        <div class="bg-moss-800 rounded-[3rem] p-12 md:p-20 text-center relative overflow-hidden">
          <!-- Background accent -->
          <div class="absolute inset-0 hex-pattern opacity-[.03] pointer-events-none"></div>
          
          <h2 class="text-section-title text-fog mb-6">Complementary Tools</h2>
          <p class="text-hero-subtitle text-moss-300 max-w-2xl mx-auto mb-16">
            Beyond the core stack, I use a variety of tools and methodologies to ensure robust, modern deployment and collaboration.
          </p>
          
          <div class="flex flex-wrap justify-center gap-4">
            {secondarySkills.map((skill) => (
              <span class="text-label-mono text-mint border border-moss-600/60 bg-moss-700/40 px-6 py-3 rounded-full hover:border-mint transition-colors cursor-default">
                {skill}
              </span>
            ))}
          </div>
        </div>
      </section>
    </div>
  </main>

  <!-- ── CTA Section ───────────────────────────────────────────────────────── -->
  <section class="bg-fog py-24 px-6 text-center">
    <div class="reveal">
      <h2 class="text-section-title text-ink mb-8">Ready to build something?<br/><em class="not-italic text-moss-500 underline decoration-mint/40 decoration-4">Let's connect.</em></h2>
      <a
        href="/#contact"
        class="inline-flex items-center gap-3 bg-moss-700 text-fog font-semibold text-sm px-10 py-4 rounded-full hover:bg-ink transition-all shadow-lg hover:shadow-2xl"
      >
        Start a conversation
        <svg width="16" height="16" viewBox="0 0 16 16" fill="none"><path d="M3 8h10M9 4l4 4-4 4" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
      </a>
    </div>
  </section>

  <Footer />
</Layout>
