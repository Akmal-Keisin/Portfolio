---
import type { PaginatedResource } from "@lib/api";

interface Props {
  meta: PaginatedResource<any>["meta"];
  links: PaginatedResource<any>["links"];
}

const { meta, links } = Astro.props;
---
<section class="bg-fog px-6 pb-24">
  <div class="max-w-6xl mx-auto flex items-center justify-between flex-wrap gap-6 border-t border-sage-200 pt-12">
    <span class="text-label-mono text-moss-400">
      Page {meta.current_page} / {meta.last_page} &nbsp;·&nbsp; {meta.total} Total
    </span>

    <div class="flex items-center gap-2">
      <a
        href={links.prev ?? undefined}
        class={`inline-flex items-center justify-center w-12 h-12 rounded-2xl border transition-all no-underline ${links.prev ? 'border-sage-200 bg-white text-moss-600 hover:bg-sage-100 hover:border-moss-400' : 'border-sage-100 bg-fog text-moss-200 pointer-events-none'}`}
        aria-disabled={links.prev === null}
      >
        <svg width="18" height="18" viewBox="0 0 14 14" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M8 3L4 7l4 4"/></svg>
      </a>

      {Array.from({ length: meta.last_page }, (_, i) => i + 1).map((p) => (
        <a
          href={`${meta.path}?page=${p}`}
          class={`inline-flex items-center justify-center w-12 h-12 rounded-2xl border text-label-mono transition-all no-underline ${p === meta.current_page ? "bg-moss-800 text-fog border-moss-800" : "border-sage-200 bg-white text-moss-600 hover:bg-sage-100 hover:border-moss-400"}`}
        >
          {p}
        </a>
      ))}

      <a
        href={links.next ?? undefined}
        class={`inline-flex items-center justify-center w-12 h-12 rounded-2xl border transition-all no-underline ${links.next ? 'border-sage-200 bg-white text-moss-600 hover:bg-sage-100 hover:border-moss-400' : 'border-sage-100 bg-fog text-moss-200 pointer-events-none'}`}
        aria-disabled={links.next === null}
      >
        <svg width="18" height="18" viewBox="0 0 14 14" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M6 3l4 4-4 4"/></svg>
      </a>
    </div>

    <span class="text-label-mono text-moss-400">
      {meta.per_page} Per Page
    </span>
  </div>
</section>
